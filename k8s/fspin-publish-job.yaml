apiVersion: batch/v1
kind: Job
metadata:
  name: fspin-publish
spec:
  template:
    spec:
      containers:
      - name: fspin-publish
        image: gcr.io/fspin36-23276e975a/fspin-publish
      restartPolicy: Never
  backoffLimit: 1
