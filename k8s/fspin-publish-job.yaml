apiVersion: batch/v1
kind: Job
metadata:
  name: fspin-publish
spec:
  template:
    spec:
      containers:
      - name: fspin-publish
        image: gcr.io/fspin37-30bd0e8528/fspin-publish
      restartPolicy: Never
  backoffLimit: 1
