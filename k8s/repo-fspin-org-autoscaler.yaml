apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: repo-fspin-org
  namespace: default
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: repo-fspin-org
  minReplicas: 1
  maxReplicas: 4
  targetCPUUtilizationPercentage: 50
